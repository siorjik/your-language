image: node:20

pipelines:
  pull-requests:
    'development':
      - step:
          name: Build checking (PR) to dev
          caches:
            - node
          script:
            - npm install -g pnpm
            - pnpm install
            - pnpm run prettier
            - pnpm build

    'main':
      - step:
          name: Build checking (PR) to main
          caches:
            - node
          script:
            - npm install -g pnpm
            - pnpm install
            - pnpm run prettier
            - pnpm build

  branches:
    development:
      - step:
          name: Merge in development
          caches:
            - node
          script:
            - echo "🧪 Runs only on merge into development"

    main:
      - step:
          name: Merge in main
          caches:
            - node
          script:
            - echo "🧪 Runs only on merge into main"
