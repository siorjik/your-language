image: node:20

pipelines:
  default:
    - step:
        name: Build checking
        caches:
          - node
        script:
          - npm install -g pnpm
          - pnpm install
          - pnpm prettier
          - pnpm build
